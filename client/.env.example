# Optional: set to your API origin when not using the Vite dev proxy.
# Leave unset during local dev so requests use same-origin `/api` and `vite.config.ts` proxy.
# VITE_API_URL=http://localhost:5000
