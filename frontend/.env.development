# Call the ASP.NET API directly in dev (avoids Vite proxy quirks and HTTPS redirect issues).
# Must match your API URL (see Properties/launchSettings.json — default http profile is port 5000).
VITE_API_URL=http://localhost:5000
